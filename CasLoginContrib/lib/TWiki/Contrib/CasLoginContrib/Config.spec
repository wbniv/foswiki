# **URL**
# Top-level URL of the CAS server
$TWiki::cfg{CAS}{casUrl} = 'https://cas.cache.denison.edu/cas';

# **URL**
# Logout URL of the CAS server (unused at the moment)
$TWiki::cfg{CAS}{casLogout} = 'https://cas.cache.denison.edu/cas/logout';

# **PATH**
# Path to the SSL certificate for the CAS server
$TWiki::cfg{CAS}{CAFile} = '/etc/ssl/certs/cascert.pem';
